# ============================================================================
# Configuration du Crawler Carzone.ie
# ============================================================================
# Copiez ce fichier vers .env et modifiez les valeurs selon vos besoins
# Ce fichier est utilisé pour la configuration locale (sans Docker)
# ============================================================================

# Configuration du proxy (optionnel)
# Format: http://hostname:port ou http://username:password@hostname:port
# Exemple: http://proxy.example.com:3128
# Exemple avec authentification: http://user:pass@proxy.example.com:3128
PROXY_URL=

# Répertoire de sortie pour les fichiers HTML
# Par défaut: /data (dans Docker) ou ./output (local)
OUTPUT_DIR=./output

# Délai entre les requêtes en millisecondes (rate limiting)
# Recommandé: 1000ms (1 seconde) minimum pour respecter le site
DELAY_MS=1000

# Timeout HTTP en millisecondes
# Temps maximum d'attente pour une requête HTTP
HTTP_TIMEOUT=15000

# Nombre de tentatives en cas d'échec
# Le crawler réessayera automatiquement en cas d'erreur réseau ou serveur (5xx)
RETRIES=3
